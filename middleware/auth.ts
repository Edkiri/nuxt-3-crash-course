// export default defineNuxtRouteMiddleware((to, from) => {
//   const isLoggedIn = true;
//   if (isLoggedIn) {
//     navigateTo(to.fullPath);
//   }

//   if (to.path !== "/") {
//     return navigateTo("/");
//   }
// });
